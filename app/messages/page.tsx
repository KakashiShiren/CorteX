import { ComingSoonPage } from "@/components/coming-soon-page";

export default function MessagesPage() {
  return (
    <ComingSoonPage
      eyebrow="Messages"
      title="Direct messaging is coming soon"
      description="The navigation item stays visible for demo completeness, but message threads are intentionally parked behind a placeholder until the full realtime experience is ready."
    />
  );
}
